import org.ifno.restquest.authorize.Basic;
import org.ifno.restquest.authorize.NTLM;

request TestService {
	config {
		method("POST");
		url("http");
	}

    header {
        auth(new Basic(login, password));
    }

    json parameters {

    }

    json response {

    }

    main {

    }
}