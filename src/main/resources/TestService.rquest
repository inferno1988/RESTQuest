import org.ifno.restquest.authorize.Basic;

request TestService {
	config {
		method("POST");
		url("http://google.com/");
	}

    header {
        auth(new Basic(login, password));
    }

    json parameters {

    }

    json response {

    }

    main {

    }
}