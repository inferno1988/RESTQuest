import org.ifno.restquest.auth.Basic;
import org.ifno.restquest.type.Json;

request TestService {
    method POST;
    url "http://google.com";

    header(String login, String password) {
        auth new Basic(login, password);
        type new Json();
    }

    data() {

    }

    response() {

    }

    main() {

    }
}